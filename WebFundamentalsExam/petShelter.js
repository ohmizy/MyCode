function hide(element) {
  element.remove ()
}

function hide(element) {
  element.remove ()
}

function picked(element) {
  alert('You picked' + element.value);
}

function like (element){
  element.innerText++;
  }